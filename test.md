# PKI 极小模型知识内化系统

## 1. 简介
这是一个测试文档，用于验证 Markdown 解析器和文本分块器是否正常工作。
在这段文字里，我们会稍微写长一点，看它怎么切分。
RAG（检索增强生成）架构的本质是带索引的文本检索，而非真正的知识内化。这导致三个根本性缺陷：
* 知识以文本片段形式存储，无法跨片段推理。
* 检索依赖向量相似度，无法表达否定、层次、因果等逻辑关系。
* 模型本身不理解这些内容，只是在 context 里临时“看到”它。

## 2. 核心架构
系统分为四个核心模块，完全本地运行，没有云端依赖。
这是为了证明真正的极客系统是长什么样子的。
