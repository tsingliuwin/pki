# PKI (Personal Knowledge Internalization)

**基于极小模型的本地个人知识内化系统**

**概念验证技术文档 · v0.2.0**

---

## 1. 项目概述

### 1.1 问题背景

现有“个人知识库”产品普遍基于 RAG（检索增强生成）架构，其本质是带索引的文本检索，而非真正的知识内化。这导致三个根本性缺陷：

* 知识以文本片段形式存储，无法跨片段推理。
* 检索依赖向量相似度，无法表达否定、层次、因果等逻辑关系。
* 模型本身不理解这些内容，只是在 context 里临时“看到”它。

> **核心洞察：**
> RAG 是“查文件柜”，不是“理解知识”。向量知识库可以被理解为一个冻结的、只有 encoder 没有 decoder 的极小模型——它只有相似度，没有逻辑结构。

### 1.2 项目目标

PKI 旨在构建一个完全本地运行的知识内化系统，将用户的文档、对话记录等非结构化内容，通过 LoRA 微调转化为一个极小模型（Qwen3-0.6B）的内化知识。核心目标实现：

* **完全本地：** 数据不出设备，无隐私泄露。
* **真正内化：** 知识编码进模型权重，而非文本检索。
* **极致轻量：** 基于纯 Rust 构建单一可执行文件（Single Binary），冷启动 < 200ms，常驻内存极低。
* **持续更新：** 增量学习，支持新内容追加。

### 1.3 与 RAG 的本质区别

| 维度 | RAG（现有方案） | PKI（本项目 v0.2.0） |
| --- | --- | --- |
| **存储形式** | 文本片段 + 向量索引 | 模型权重（GGUF 格式的 LoRA adapter） |
| **检索方式** | 向量相似度查找 | 模型推理（前向传播） |
| **跨文档推理** | 不支持 | 支持（知识已融合） |
| **底层架构** | 依赖向量数据库与庞大的 Python 栈 | 纯 Rust 编译，单文件无依赖，极致内存安全 |
| **更新成本** | 低（append 向量） | 中（后台静默增量 LoRA 训练） |

---

## 2. 系统架构

系统演进为“双引擎”模式，明确区分“运行态”与“训练态”：

### 2.1 整体流程 (Pipeline)

1. **原始内容摄入 (Rust)：** 高速解析文档并进行语义分块。
2. **QA 生成与过滤 (Rust)：** 调用本地模型生成 QA 对，并使用 LLM-as-a-Judge 机制过滤低质数据。
3. **LoRA 训练编排 (Rust 唤起 Python)：** 隐式唤起后台 Unsloth Worker 进行微调，并导出为 `.gguf` 格式。
4. **本地推理基座 (Rust)：** 核心引擎热加载个人 Adapter，提供极速 CLI 交互。

### 2.2 模块详解

**模块一：数据摄入器 (`pki-ingest`)**
纯 Rust 实现，彻底摆脱 Python 脆弱的环境依赖。

* **文档解析：** 使用 `pdf-extract`、`pulldown-cmark` 等 Rust crate 实现对 PDF、Markdown、HTML 的极速无损解析。
* **语义分块：** 使用 `text-splitter`，结合 Tokenizer 确保分块不截断语义。
* **去重机制：** 基于内容 hash，避免重复训练同一知识。

**模块二：QA 生成与清洗 (`pki-ingest/qa_gen`)**

* **生成器：** 在 Rust 进程内直接通过 `llama-cpp-2` 加载本地模型，每块生成覆盖事实、推理、应用三类问题的 QA 对。
* **LLM-as-a-Judge 过滤：** 自动引入打分机制，剔除答案过短、问题无意义的样本，确保“高质量数据进，高质量权重出”。

**模块三：训练编排器 (`pki-trainer`)**

* **Base model：** 降维至极致轻量的 **Qwen3-0.6B-Instruct**。
* **训练框架：** 保持使用 Unsloth + TRL 进行 QLoRA 训练。由 Rust 主程序通过子进程方式静默唤起 Python 脚本执行。
* **产物转换：** 训练完成后，自动执行 `convert-lora-to-ggml.py`，将 adapter 转换为 `ggml` 格式供底层 C++ 算子调用。

**模块四：本地推理基座 (`pki-engine`)**

* **核心引擎：** 基于 `llama-cpp-2` 构建的安全 Rust 绑定，实现零内存拷贝推理。
* **性能表现：** 在 CPU 上实现百毫秒级首字响应，推理过程完全断网、数据不出设备。

---

## 3. 技术选型与性能预期

### 3.1 核心依赖栈

| 组件 | 选型 | 理由 |
| --- | --- | --- |
| **Base Model** | **Qwen3-0.6B-GGUF** | 参数减少 60%，原生 GGUF 格式加载零延迟，极度节省内存。 |
| **推理底座** | **Rust + `llama-cpp-2**` | 高并发、低延迟，彻底告别 Python GIL 限制。 |
| **训练引擎** | Unsloth + TRL | 高效生成 adapter，通过后台子进程集成。 |
| **内容解析** | `pdf-extract`, `pulldown-cmark` | 纯 Rust 生态，内存安全，解析极快。 |

### 3.2 硬件与性能预期

| 配置 | 预期内存占用 | 推理首字延迟 (TTFT) | 训练时间 (1000条QA) |
| --- | --- | --- | --- |
| **仅 CPU (纯本地常驻)** | **< 800 MB** | **< 200 毫秒** | 约 40-60 分钟 |
| **GPU 4GB VRAM** | < 1 GB | < 50 毫秒 | 约 5-10 分钟 |
| **Apple M系列芯片** | < 1 GB (共享内存) | < 100 毫秒 | 约 10-15 分钟 |

---

## 4. 项目结构 (Cargo Workspace)

系统采用 Rust Workspace 模式进行高内聚拆分：

```text
pki/
├── Cargo.toml                  # Workspace 统筹配置
├── pki-cli/                    # (前端) 用户交互层，基于 clap
│   └── src/main.rs             
├── pki-ingest/                 # (核心组件 1) 数据摄入、解析与 QA 生成
│   └── src/lib.rs              
├── pki-engine/                 # (核心组件 2) 基于 llama-cpp-2 的推理基座
│   └── src/lib.rs              
└── pki-trainer/                # (边界组件) 唤起并编排 Unsloth Python 训练逻辑
    ├── src/orchestrator.rs     
    └── python_scripts/         # 隐藏的微调与 GGUF 导出脚本

```

---

## 5. 挑战与缓解策略

| 风险点 | 描述 | 缓解策略 |
| --- | --- | --- |
| **灾难性遗忘** | 新训练覆盖旧知识 | 构建核心知识向量索引，精准抽取 20% 易冲突旧数据进行经验回放；采用权重融合 (Weight Merging) 结合新旧 Adapter。 |
| **格式转换壁垒** | Unsloth 产出与 Rust GGUF 推理的对接 | 训练管线末端强制集成 `llama.cpp` 的 `convert-lora-to-ggml` 脚本。 |
| **知识容量上限** | 0.6B 模型参数能内化的知识量极其有限 | 分领域构建多个 Adapter（如“工作”、“学习”），利用 Rust 引擎极低延迟的特性实现毫秒级动态切换挂载。 |
| **评估难以量化** | 人工验证效率极低 | 构建全自动化 CI/CD 评估流，使用 LLM-as-a-judge 盲测打分。 |

---

## 6. 产品化路径与长期愿景

### 6.1 演进路径

若核心引擎（`pki-engine`）与双引擎架构验证成功，优先落地的产品形态为：

1. **极简常驻 Daemon：** 作为后台系统级服务隐式运行，内存占用极低（< 800MB）。无需暴露对外 API，仅通过本地管道或极简的命令行指令即可唤醒查询。
2. **沉浸式 TUI (终端用户界面)：** 引入 Rust 生态中的 `ratatui` 库，在终端内构建极客风格的图形化交互界面。用户可以直接在终端内完成文档导入进度查看、对话查询以及 Adapter 状态监控，无需离开终端环境。
3. **纯粹单体分发：** 最终编译为体积小巧的单一可执行文件（`pki.exe` / `pki`）。实现真正的“零依赖、双击即用、阅后即焚（若需销毁）”。

### 6.2 长期愿景

PKI 的终极形态是一个“第二大脑”的真正实现——不是把你的文件存起来供检索，而是把你读过的、写过的、经历过的，真正转化为一个只属于你的微小智能，随时可以被“问出来”。

> **核心价值主张：**
> “你的知识，真正属于你。” 完全脱网，单文件闭环，纯净运行。没有云端，没有订阅，没有隐私暴露风险。一个越用越懂你的极客专属模型。
